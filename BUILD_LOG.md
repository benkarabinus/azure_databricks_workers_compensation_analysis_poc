# Build Log — State Fund Lane 1 POC

> **Purpose:** Working progress log so any new session (or a compacted context window) can resume the build without re-deriving decisions. This is a **working doc**, not the published `CHANGELOG.md`. Update it at the end of each meaningful unit of work: what changed, why, and what's next.

## Current status

- **Phase:** Customization complete; **scaffolding in progress (steps 1–4 done).**
- **Local env:** project **`.venv`** (Python 3.12) holds data-gen/dev deps from `requirements.txt`. Do NOT install into system Python. Activate with `.venv\Scripts\python`.
- **Primary instructions:** `.github/copilot-instructions.md` is the source of truth for structure, sessions, architecture, and conventions.
- **Reference (do not ship):** `state-fund-lane1-poc-implementation-plan.md` — full design (ERD, column lineage, sample data, ML plan). Keep for scaffolding context.

## Locked decisions

- **Use case:** California State Fund Lane 1 POC — RTW duration prediction (regression) + claims fraud investigation triage (classification). Frame fraud as *triage acceleration*, never "automated fraud detection."
- **Platform:** Azure Databricks **Serverless** only (no classic clusters). Medallion Bronze → Silver → Gold.
- **Unity Catalog:** single catalog `state_fund_poc`; schemas `bronze`, `silver`, `gold`, `config`, `security`, `ml`. Bronze tables prefixed `raw_`.
- **Storage:** dedicated ADLS Gen2 registered as a UC External Location (separate from metastore root).
- **Repo style:** CalmVault-like tutorial — `README.md` (landing), `DEPLOYMENT_GUIDE.md` (full walkthrough), `docs/` Jekyll Pages site (just-the-docs), session-per-folder.
- **Databricks App:** **Streamlit** (reads `gold.fraud_scores` only).
- **Synthetic data:** Python pandas/Faker, seeded; inject ~2–5% dirty patterns.
- **Docs rule:** every session step must cite verified Microsoft Learn / Databricks docs (no guessed URLs); keep `session-N/README.md`, `docs/session-N.md`, and `DEPLOYMENT_GUIDE.md` in sync.
- **Diagrams:** `.drawio` sources under `docs/diagrams/` + exported `.svg`/`.png`, Azure/Databricks icons; reference exported image in Markdown.

## Customization layer (DONE)

- `.github/copilot-instructions.md` — promoted primary instructions.
- `.github/instructions/pyspark.instructions.md` — `**/*.py`.
- `.github/instructions/sql.instructions.md` — `**/*.sql`.
- `.github/instructions/diagrams.instructions.md` — `**/*.drawio, docs/diagrams/**`.

## Planned build order (TODO)

1. ✅ `common/config.py` + `data/generate_synthetic_data.py` (+ `requirements.txt`, `.gitignore`, `.venv`). Dataset generated (5,000 claims).
2. ✅ `README.md` + `DEPLOYMENT_GUIDE.md` skeletons; `docs/` Jekyll site (`_config.yml`, `Gemfile`, `index.md`).
3. ✅ Session 0 — Terraform IaC (`session-0-setup/terraform/`) + README + `docs/session-0.md` + DEPLOYMENT_GUIDE fill-in. Provisions serverless workspace + dedicated ADLS Gen2 UC External Location.
4. ✅ Session 1 — Bronze: `00_create_catalog_and_schemas.sql` (catalog + MANAGED LOCATION + 6 schemas + `bronze.landing` external volume), `bronze_autoloader_pipeline.sql` (Lakeflow SQL pipeline: `STREAMING TABLE` + `read_files` → 5 CSV/JSON raw_* streaming tables), `ingest_adjuster_notes_excel.py` (openpyxl serverless job → raw_adjuster_notes). UI-walkthrough README + `docs/session-1.md` + DEPLOYMENT_GUIDE fill-in.
5. ✅ Session 2 — Silver: `security_masking_functions.sql` (UC masks + row filter), `silver_pipeline.sql` (5 entity MVs + QUALIFY dedupe + Expectations + inline masks/row filter), `silver_adjuster_notes_ai.sql` (ai_query/ai_classify/ai_extract), `grants_and_validation.sql`. README + `docs/session-2.md` + DEPLOYMENT_GUIDE fill-in.
6. ✅ Session 3 — Gold: `gold_pipeline.sql` (rtw_features/fraud_features/rtw_outcomes_summary MVs), `data_quality_view.sql` (gold.data_quality over the Silver event log), `genie_space.md`, `dashboard/rtw_fraud.lvdash.json`. README + `docs/session-3.md` + DEPLOYMENT_GUIDE fill-in.
7. ✅ Session 4 — ML: serverless scikit-learn training + MLflow, register to UC (`train_rtw_model.py`, `train_fraud_model.py`, `register_models.py`). README + `docs/session-4.md` + DEPLOYMENT_GUIDE fill-in.
8. ✅ Session 5 — Serving: batch scoring (`batch_score_fraud.py` → gold.fraud_scores, `batch_score_rtw.py` → gold.rtw_predictions), `vector_search_setup.py` (AI Search index over redacted notes), Streamlit Databricks App (`app/`), `workflow/end_to_end_job.yaml`, `governance/` (audit/row_filter/time_travel). README + `docs/session-5.md` + DEPLOYMENT_GUIDE fill-in.
9. Architecture diagram(s) under `docs/diagrams/`.

## Log

- **2026-06-28** — Session 5 `batch_score_fraud.py` reconciled with workspace-tested code (skops load fix). Root cause: Session 4 training logs models with `skops_trusted_types=["numpy.dtype"]`, so mlflow serializes the sklearn pipeline via **skops** (not pickle); loading the **HistGradientBoosting** champion fails because skops needs the internal loss type trusted and the Cython `_loss` module resolves by short name. Fix: `%pip install mlflow[databricks] scikit-learn==1.7.2`; `sys.modules.setdefault('_loss', sklearn._loss._loss)`; download artifacts, read `MLmodel` sklearn flavor `pickled_model`, `skops.io.load(..., trusted=["_loss.CyHalfBinomialLoss","numpy.dtype"])`. **Update:** `batch_score_rtw.py` only needed the `scikit-learn==1.7.2` pin — its champion loads fine via `mlflow.sklearn.load_model` (so the RTW champion is RandomForest, not HistGradientBoosting). **Watch:** `register_models.py` validation (`mlflow.pyfunc.load_model`) may still need skops handling for the fraud (HGB) champion.
- **2026-06-28** — Session 5 workflow def changed **JSON → YAML** (`workflow/end_to_end_job.yaml`): the Databricks Jobs UI imports YAML via Job ▸ kebab (...) ▸ **Edit as YAML**, not JSON. Same task graph (ingest → bronze → silver → gold → score → quality, serverless env, `<...>` placeholders). Deleted the `.json`. Synced README/`docs/session-5.md`/DEPLOYMENT_GUIDE/copilot-instructions (structure).
- **2026-06-27** — Session 5 (step 8) Serving built (user forks: score existing Gold tables [simple, predicted-vs-actual]; include Vector Search). `session-5-serving/`: (1) `batch_score_fraud.py` — load `ml.fraud_model@champion` (mlflow.sklearn), predict_proba on gold.fraud_features, risk_tier (High≥0.7/Med≥0.4/Low) + heuristic top_contributing_factor, write **gold.fraud_scores** (+ is_fraud_actual for the demo). (2) `batch_score_rtw.py` — `ml.rtw_model@champion` → **gold.rtw_predictions** (predicted vs actual days_to_rtw). (3) `vector_search_setup.py` — CDF table gold.claim_notes from silver.adjuster_notes → AISearchClient (`databricks-ai-search`; older = databricks-vectorsearch/VectorSearchClient) create_endpoint STANDARD + create_delta_sync_index (TRIGGERED, primary_key claim_id, embedding_source_column note_text_redacted, embedding_model_endpoint_name databricks-gte-large-en) → similarity_search. (4) `app/` Streamlit Databricks App — app.py reads gold.fraud_scores via databricks-sql-connector + SDK Config (warehouse via `DATABRICKS_WAREHOUSE_ID` from a `sql-warehouse` app resource), app.yaml (command streamlit run app.py + env valueFrom), requirements.txt. (5) `workflow/end_to_end_job.json` — reference Job: ingest_excel + bronze_pipeline → silver → gold → score_fraud/score_rtw → data_quality (serverless environment_key; `<...>` placeholders for pipeline IDs/notebook paths). (6) `governance/` — audit_system_tables.sql (system.access.audit + table_lineage), row_filter_demo.sql (DESCRIBE EXTENDED + visible/Southern-CA counts + is_account_group_member), time_travel_recovery.sql (DESCRIBE HISTORY + VERSION AS OF + RESTORE on gold.fraud_scores). Verified via MS Learn: AI Search SDK (databricks-ai-search/AISearchClient), Databricks Apps SQL pattern. NOT yet run in workspace — watch: AI Search package/region + endpoint ONLINE wait, Apps SQL warehouse resource key + SP grants, job environments `spec.client` version, system.access enablement. Added gold tables rtw_predictions/data_quality/claim_notes to copilot-instructions + README UC layout. Synced README/`docs/session-5.md` (nav_order 8)/DEPLOYMENT_GUIDE.
- **2026-06-27** — Session 4 (step 7) ML built as **serverless scikit-learn training + MLflow** (NOT AutoML — user-confirmed fork: verified via MS Learn that AutoML regression/classification needs a classic DBR-ML cluster, is unsupported on serverless, and is removed in DBR 18.0 ML). `session-4-ml/`: (1) `train_rtw_model.py` (serverless notebook) — loads `gold.rtw_features` (closed claims), ColumnTransformer (OHE cats + impute/median nums), candidates RandomForestRegressor + HistGradientBoostingRegressor, logs each run to MLflow exp `/Users/<user>/state_fund_poc_rtw` with rmse/mae/r2 + signature/input_example, best by RMSE. (2) `train_fraud_model.py` — loads `gold.fraud_features` (labeled subset), all-numeric + StandardScaler, candidates RandomForest(class_weight balanced)/HistGB/LogisticRegression(balanced), stratified split, logs pr_auc/roc_auc/f1/precision/recall, best by **PR-AUC** (imbalance). (3) `register_models.py` — `set_registry_uri('databricks-uc')`, search_runs best per experiment (rmse asc / pr_auc desc), `register_model` → `state_fund_poc.ml.rtw_model` / `.fraud_model`, set `@champion` alias + description, validate via pyfunc load + predict sample. DESIGN: training and registration separated (clean MLOps); experiments under `/Users/<current_user>/...` so all 3 notebooks must run as same user; sklearn-only (RF+HistGB built-in, no extra install) stands in for AutoML multi-algo search; `OneHotEncoder(sparse_output=False)` assumes sklearn>=1.2. NOT yet run in workspace — watch: serverless sklearn/mlflow versions, current_user() experiment path writability, CREATE MODEL on ml schema. Renamed files from the planned `train_*_automl.py`. Synced README/`docs/session-4.md` (nav_order 7)/DEPLOYMENT_GUIDE/copilot-instructions (structure + Sessions bullet + ML data-flow).
- **2026-06-27** — Genie instructions now also a **navigable docs page**: created `docs/session-3-genie.md` as a just-the-docs **child** of `docs/session-3.md` (`has_children: true` on the parent; `parent: Session 3 — Gold`) mirroring `session-3-gold/genie_space.md` (repo file kept per request). Linked it from the Session 3 Genie step; added a cross-ref note in the repo file + a copilot-instructions rule for mirrored `docs/` child pages.
- **2026-06-27** — Session 3 (step 6) Gold built as a **Lakeflow SQL pipeline** + interactive quality view + Genie/dashboard config (consistent SQL-SDP pattern). `session-3-gold/`: (1) `gold_pipeline.sql` (plain .sql pipeline source, target catalog state_fund_poc / schema gold) — 3 MVs: `rtw_features` (one row per CLOSED claim, label `days_to_rtw`; derives age_band from dob@injury, tenure_years=injury−hire, prior_claims_count window, treatment rollups from silver.treatments, days_to_first_treatment/rtw from silver.rtw_timeline; `WHERE rtw_date IS NOT NULL`), `rtw_outcomes_summary` (injury_type×region KPIs from rtw_features), `fraud_features` (one row per LABELED claim = inner join to `bronze.raw_siu_labels`; billing_total/distinct_providers/provider_claim_count_30d from silver.provider_billing, note_fraud_signal/attorney_flag from silver.adjuster_notes, days_injury_to_report, weekend_injury_flag, prior_claims_count, label is_fraud). Reads silver.* + bronze.raw_siu_labels (labels = ground-truth reference joined at feature layer). (2) `data_quality_view.sql` (interactive) — `gold.data_quality` view via `event_log(TABLE(state_fund_poc.silver.claims))`, explodes `details:flow_progress:data_quality:expectations` for pass/fail + pass_rate_pct (latest update). (3) `genie_space.md` — Genie space config (3 Gold tables, domain instructions framing fraud as triage, 7 sample NLQ). (4) `dashboard/rtw_fraud.lvdash.json` — STARTER AI/BI dashboard (3 datasets + counter/bar/table widgets; import-and-refine). KEY: Gold pipeline reads silver.claims (row filter) → **owner must be in pii_authorized** (same as Silver) or Southern CA dropped. Verified via MS Learn: event_log TVF + expectations schema; .lvdash.json import format. NOT yet run in workspace — watch: event_log(TABLE()) view perms (run-as scoped), .lvdash.json widget specs on import, provider_claim_count_30d self-join cost. Synced README/`docs/session-3.md` (nav_order 6)/DEPLOYMENT_GUIDE/copilot-instructions (structure + Sessions bullet + Gold data-flow).
- **2026-06-27** — Session 2 fix: the `MASK` / `WITH ROW FILTER` function references in `silver_pipeline.sql` must use the **fully-qualified 3-part name** (`state_fund_poc.security.mask_ssn` / `mask_dob` / `claims_region_filter`) — the pipeline default catalog/schema is not applied to mask/filter function refs (caught running in the workspace). Updated the file + added a convention bullet to `sql.instructions.md`.
- **2026-06-27** — Session 2 (step 5) Silver built as a **Lakeflow SQL pipeline** + interactive governance SQL (design confirmed with user: SQL pipeline, `ai_query` redaction, MV + `QUALIFY` dedupe). `session-2-silver/`: (1) `security_masking_functions.sql` (interactive SQL notebook) — `security.mask_ssn` (last-4), `security.mask_dob` (birth-year), `security.claims_region_filter` (hides Southern CA), all gated on `is_account_group_member('pii_authorized')`; run BEFORE the pipeline. (2) `silver_pipeline.sql` (plain .sql pipeline source) — 5 MVs: `claims` (3 date-format parse, normalize, cast, null invalid SSNs, name normalize, dedupe via `QUALIFY ROW_NUMBER() OVER(PARTITION BY claim_id ORDER BY _ingested_at DESC)=1`, **inline column masks on ssn/dob + WITH ROW FILTER on region**, Expectations: claim_id FAIL UPDATE, amount>0 + injury_date<=today DROP ROW, ssn warn), `employees` (wage_band, drop full_name), `treatments` (explode events[], is_surgery, exclude return_to_work), `provider_billing` (explode billing_lines[]), `rtw_timeline` (injury→first_treatment→rtw from events, reads `claims`). (3) `silver_adjuster_notes_ai.sql` (plain .sql) — `ai_query` ('databricks-meta-llama-3-3-70b-instruct') redaction + `ai_classify`(routine/potential_fraud/disputed) + `ai_extract`(injury_severity/fraud_indicators/attorney) → `silver.adjuster_notes` (stores redacted text only). (4) `grants_and_validation.sql` (interactive) — grants to `analysts`/`pii_authorized` + masked-vs-unmasked validation + poison/dup checks. KEY DECISIONS (verified via MS Learn): MV masks/filters MUST be declared in the `CREATE` statement (cannot `ALTER` a view/MV); the row-filter UDF evaluates `is_account_group_member` as the **invoker**, so the **pipeline owner must be in `pii_authorized`** or downstream tables (rtw_timeline, Gold) silently lose Southern CA rows — documented prominently. Account groups `analysts`/`pii_authorized` are a manual account-console prereq. `ai_query` needs a serving endpoint (confirm/replace name). Synced README/`docs/session-2.md` (nav_order 5)/DEPLOYMENT_GUIDE/copilot-instructions (structure + Sessions bullet + Silver data-flow). NOT yet run in a workspace — watch on first run: `ai_query` endpoint name, `LATERAL VIEW explode` on inferred JSON arrays, and `try_to_date('dd-MMM-yyyy')` case-insensitive month parse.
- **2026-06-26** — Bronze pipeline → **Lakeflow SQL (SDP)**. Replaced `bronze_autoloader_pipeline.py` (`import dlt` / `@dlt.table`) with `bronze_autoloader_pipeline.sql` per the [Develop Lakeflow SDP code with SQL](https://learn.microsoft.com/azure/databricks/ldp/developer/sql-dev) docs: one `CREATE OR REFRESH STREAMING TABLE` per source reading `FROM STREAM read_files(...)` (Auto Loader invoked from SQL). CSV uses `inferColumnTypes => false` (all-strings, as-landed); JSON keeps nested structure; each adds `_source_file` (`_metadata.file_path`) + `_ingested_at` (`current_timestamp()`). Unqualified names resolve to the pipeline target catalog `state_fund_poc` / schema `bronze`; landing root parameterized via a pipeline **Configuration** key-value (`landing_path : /Volumes/state_fund_poc/bronze/landing`) referenced with `${landing_path}` — Serverless SDP does **not** support a `SET` statement in source. Excel ingest stays Python. Deleted the old `.py`. Authored as a plain `.sql` source file (SDP convention — pipelines use `.sql`/`.py` source files, not notebooks). Synced session README, `docs/session-1.md`, DEPLOYMENT_GUIDE, copilot-instructions (structure + Bronze data-flow), and the pyspark/sql instruction files (added a **Lakeflow SDP (SQL)** section to sql.instructions). User confirmed the serverless workspace (azapi) provisioned fine and catalog/schemas created.
- **2026-06-26** — Session 0 workspace → **true serverless** (azapi swap). The `azurerm_databricks_workspace` resource only creates classic ("Hybrid") workspaces (managed RG/VNet/DBFS in your subscription). Switched `workspace.tf` to `azapi_resource "workspace"` (`type = Microsoft.Databricks/workspaces@2026-01-01`, `body.properties.computeMode = "Serverless"`, `sku.name = "premium"`, `parent_id = local.resource_group_id`, `response_export_values = ["properties.workspaceUrl"]`) — no managed RG/VNet/DBFS; UC auto-enabled; external locations fully supported. Added the `azapi` provider (`Azure/azapi ~> 2.0`, installed v2.10.0) to `versions.tf` + a `provider "azapi"` block (`subscription_id = var.subscription_id`). Rewired `providers.tf` databricks `azure_workspace_resource_id` → `azapi_resource.workspace.id` and `outputs.tf` `workspace_url` → `https://${azapi_resource.workspace.output.properties.workspaceUrl}` / `workspace_id` → `azapi_resource.workspace.id`. **Two-phase first apply target is now `terraform apply -target='azapi_resource.workspace'`** (then `terraform apply`). `terraform init` + `validate` pass. Done greenfield (state was empty after destroy) so no state surgery. Synced README, docs/session-0.md, DEPLOYMENT_GUIDE, copilot-instructions. **Watch on first apply:** confirm `sku = { name = "premium" }` is accepted for a serverless workspace (adjust/remove if the ARM API rejects it). `session-1-bronze/`: (1) `00_create_catalog_and_schemas.sql` — `CREATE CATALOG state_fund_poc MANAGED LOCATION '<managed_catalog_location>'` + 6 schemas + `CREATE EXTERNAL VOLUME bronze.landing LOCATION '<landing_path>'` (placeholders = Session 0 outputs); (2) `bronze_autoloader_pipeline.py` — Lakeflow/DLT `import dlt`, Auto Loader `cloudFiles` loop over 5 sources (claims/hr/siu_labels CSV + treatments/billing JSON) → `raw_*` streaming tables with `_source_file` (`_metadata.file_path`) + `_ingested_at`, CSV kept as-strings; (3) `ingest_adjuster_notes_excel.py` — `%pip install openpyxl` + `restartPython`, pandas `read_excel(dtype=str)` → overwrite `bronze.raw_adjuster_notes` (idempotent). Decisions (approved): external **volume** for landing (best practice) read at `/Volumes/state_fund_poc/bronze/landing/<source>`; **UI upload** to the volume; Excel **overwrite**; Databricks **source-format .py** files. Updated `common/config.py` `DEFAULT_LANDING_PATH` → volume path (computed from catalog) — verified resolves. Added the missing **`raw_siu_labels`** to the copilot-instructions bronze table list (6 bronze tables, not 5). Every UI step cites verified MS Learn docs. README/`docs/session-1.md`/DEPLOYMENT_GUIDE in sync.
- **2026-06-24** — Session 0 polish (post clean teardown/redeploy ×2): removed the now-unused `random` provider from `versions.tf` (state empty, lock file re-initialized, `terraform validate` passes). Docs: bumped the role-assignment count to ×4 (added the deployer blob-data grant) and added two design bullets — **file events** (the connector's EventGrid role makes UC auto-create an Event Grid system topic for Auto Loader notifications) and **`force_destroy`** on external locations (teardown not blocked by soft-deleted managed tables); fixed a missing newline before the "Dedicated storage" bullet. `DEPLOYMENT_GUIDE.md` Session 0 step 2 now lists the required deterministic names and step 3 is the two-phase + account-admin flow. copilot-instructions provider list → (azurerm, databricks, time). Decision: keep the local `terraform/.gitignore` separate (TF convention + self-contained sessions). Skipped a troubleshooting section by request.
- **2026-06-24** — Session 0 teardown gotchas (operational): (1) A stale `~/.databrickscfg` DEFAULT profile (old workspace, different tenant) kept hijacking the databricks provider whenever it had no in-state workspace to target (during destroy/redeploy), causing cross-tenant `Invalid access token` / "external user" errors. Fix: retire it — `Move-Item $HOME/.databrickscfg $HOME/.databrickscfg.bak-<ts>`. This tutorial uses pure azure-cli auth, so the profile is pure liability. (2) `terraform destroy` of the RG failed (~10 min retry) because an **orphan workspace** existed in the RG but not in state; azurerm won't delete a non-empty RG by default. Fix: delete the orphan (`az resource delete --ids <workspace-id>`), then `terraform destroy`. NOTE: do NOT globally set `prevent_deletion_if_contains_resources = false` — it would force-delete a customer's existing RG when `create_resource_group = false`. (3) Deleting a workspace does NOT purge metastore-level UC objects (storage credential `ac-state-fund-poc`, external locations `sfpoc_landing`/`sfpoc_managed`) — they can orphan in the regional metastore and cause name conflicts on redeploy; clean via Catalog → External Data. State is now empty (clean slate).
- **2026-06-24** — Session 0 deterministic naming: removed the `random_string.suffix` and all autogenerated names. `storage_account_name`, `workspace_name`, and `access_connector_name` are now **required** variables (validated); access connector no longer derives from prefix. Containers renamed to `landing` + `state-fund-poc-managed` (Azure container rule: lowercase letters/digits/hyphens, no uppercase/underscores) with validation. tfvars/example now document each name's constraints; restored the placeholder `subscription_id` in tfvars.example (a real value had leaked into the committed example). `prefix` is retained only for the external location names (`<prefix>_landing/_managed`). The `random` provider is still declared in versions.tf (unused) so `terraform destroy` can clean up the in-state `random_string`; remove it after the rebuild. Renaming forces replacement of storage account + workspace + access connector → do `terraform destroy` then `terraform apply`. `terraform validate` passes.
- **2026-06-24** — Session 0 external-location 403 fix: Databricks validates a new `databricks_external_location` by test-accessing the path as the access connector's managed identity, which raced ahead of Azure RBAC propagation (`Storage Blob Data Contributor` on the account) → `403 not authorized`. Added `time_sleep.wait_for_connector_roles` (300s, depends on the connector's blob/account role assignments) and gated both external locations on it. `terraform validate` passes. Levers if it still races: raise `create_duration` or set `skip_validation = true` temporarily.
- **2026-06-24** — Session 0 deploy fixes (env-specific): (1) Databricks provider pinned `auth_type = "azure-cli"` so a stale `~/.databrickscfg` PAT can't shadow Azure CLI auth (was causing `Invalid access token` on `databricks_storage_credential`). (2) Subscription policy forbids shared-key storage auth → set provider `storage_use_azuread = true`, account `shared_access_key_enabled = false` + `default_to_oauth_authentication = true`, added `azurerm_client_config` + a `Storage Blob Data Contributor` role assignment for the deployer with a 150s `time_sleep` propagation wait (added `hashicorp/time` provider). First-run note: workspace must exist before UC resources (two-phase `apply -target=azurerm_databricks_workspace.this` then `apply`; quote the target in PowerShell). Docs (README + docs/session-0.md) updated with auth notes + Entra-ID storage design. `terraform validate` passes.
- **2026-06-23** — Session 0 storage redesign (per UC best practices): the dedicated ADLS Gen2 account now hosts **two containers** — `landing` (raw files → `<prefix>_landing` external location) and `managed` (catalog-level managed storage → `<prefix>_managed` external location). Bronze/Silver/Gold will be **managed tables** in separate schemas; Session 1's `CREATE CATALOG state_fund_poc MANAGED LOCATION 'abfss://managed@…/state_fund_poc'` uses the `managed_catalog_location` output. Renamed the storage account resource `landing → this`. Added optional `storage_account_name` / `workspace_name` override vars (coalesce over generated names; generated infix `land → lake`) and a `managed_container` var. New outputs `managed_external_location_url`, `managed_catalog_location`. `terraform fmt`/`validate` pass. Synced README, docs/session-0.md, DEPLOYMENT_GUIDE, and copilot-instructions Repo Structure + Session 0 bullet. **Session 1 TODO:** create catalog with MANAGED LOCATION; bronze/silver/gold as separate schemas.
- **2026-06-22** — Session 0 (step 3): replaced the original manual/SQL setup with **Terraform IaC** under `session-0-setup/terraform/` (versions/providers/variables/main/storage/workspace/unity_catalog/outputs + tfvars.example + .gitignore). Provisions a Premium (serverless-capable, UC auto-enabled) workspace + dedicated ADLS Gen2 account registered as a UC External Location via an access-connector managed identity. Decisions: full IaC incl. external location (azurerm + databricks providers); resource-group create-or-reuse toggle (`create_resource_group`); Entra groups/grants deferred; region default westus2. `terraform fmt`/`init -backend=false`/`validate` all pass (azurerm v4.78, databricks v1.118). Authored `session-0-setup/README.md` + `docs/session-0.md`, filled the DEPLOYMENT_GUIDE Session 0 section, and updated copilot-instructions Repo Structure + Sessions bullet. Cited verified MS Learn / Databricks / Terraform Registry docs throughout.
- **2026-06-19** — Data dictionary: created `docs/data-dictionary.md` (Bronze→Silver→Gold overview, source-file table, Mermaid ERD of Silver hub-and-spoke, layer-by-layer dictionary, Gold feature-table detail, "why Gold"). Enabled Mermaid in `docs/_config.yml`. Added concise `data/README.md` (file listing + regenerate instructions, links to the dictionary). Linked both from `README.md` and `docs/index.md`. Sourced from implementation plan §2.
- **2026-06-19** — Step 2: replaced `README.md` with the tutorial landing page (About / Session Structure / Architecture / Quick Start). Created `DEPLOYMENT_GUIDE.md` skeleton (audience/prereqs, data-gen step, per-session goal/output with steps marked _coming soon_). Created `docs/` Jekyll Pages site: `_config.yml` (just-the-docs remote_theme, baseurl = repo name), `Gemfile` (github-pages + webrick), `index.md` (home). Per-session `docs/session-N.md` pages will be authored alongside each session.
- **2026-06-19** — Generated the full synthetic dataset into `data/` at 5,000 claims (seed 42): claims_core.csv (5,101 rows w/ dupes+poison), hr_records.csv (4,000), medical_treatments.json (5,000, ~4 MB), provider_billing.json (5,000, ~2.2 MB), adjuster_notes.xlsx (2,760), siu_labels.csv (2,260 labeled, 232 fraud ≈ 10.3%). ~7 MB total.
- **2026-06-19** — Step 1: created `common/config.py` (catalog/schema/table/path helpers), `common/__init__.py`, `data/generate_synthetic_data.py` (seeded pandas/Faker generator for all six sources w/ ~2-5% dirty patterns, nested JSONL, return_to_work events, SIU labels). Added `requirements.txt` + `.gitignore`. Created project `.venv` (Python 3.12); reverted accidental system-Python (arm64) installs. Smoke-tested generator at 100 claims — all six files produced.
- **2026-06-19** — Authored & promoted `.github/copilot-instructions.md`; aligned pyspark/sql instruction files; added diagrams instruction file; created this build log + repo memory. Scaffolding pending.
